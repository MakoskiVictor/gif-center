import { Link } from "wouter";
export default function Card(/* { id, url } */) {
  return (
    <div>
      {/* <Link to={`/gif/${id}`}>
        <img
          className={styles.gifImages}
          key={index}
          role='gifImage'
          src={`${g.url}`}
        />
      </Link> */}
    </div>
  );
}
